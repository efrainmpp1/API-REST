const {hash , compare} = require('bcrypt');
const {v4} = require('uuid');
const User = require('../models/User');

//Services que o controllers de User vão pedir

class UserServices {

};  

module.exports = new UserServices;